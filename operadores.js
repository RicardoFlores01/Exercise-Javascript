// OPERADORES TERNARIOS, ? devuelve un true, : devuelve false
let age = prompt("Ingresa tu edad")

let isAdult = age >= 18
				? 'Eres un mayor de edad'
				: 'Aun eres un nino'

alert(isAdult)